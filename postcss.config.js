// FILE: postcss.config.js — PostCSS pipeline that compiles Tailwind into app/globals.css.
// Depends on: tailwindcss, autoprefixer.

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
